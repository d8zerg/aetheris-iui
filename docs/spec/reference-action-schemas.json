[
  {
    "id": "camera.search",
    "version": "1.0.0",
    "parameters": {"type": "object", "required": ["query"], "properties": {"query": {"type": "string"}}},
    "reversibility": "reversible",
    "blastRadius": {"class": "scoped", "maxEntities": 0},
    "idempotencyKey": "id + query",
    "dryRun": "not_applicable",
    "sideEffects": "read_only",
    "requiredScopes": ["camera.read"],
    "confirmation": "automatic",
    "rollback": "rollback_api",
    "examples": [{"intent": "Find lobby cameras", "parameters": {"query": "lobby"}}],
    "validationRules": []
  },
  {
    "id": "camera.disable",
    "version": "1.0.0",
    "parameters": {"type": "object", "required": ["cameraId"], "properties": {"cameraId": {"type": "string"}}},
    "reversibility": "reversible",
    "blastRadius": {"class": "bounded", "maxEntities": 1},
    "idempotencyKey": "id + cameraId",
    "dryRun": "mandatory",
    "sideEffects": "writes_system",
    "requiredScopes": ["camera.write"],
    "confirmation": "typed",
    "rollback": "rollback_api",
    "examples": [{"intent": "Disable camera 42", "parameters": {"cameraId": "42"}}],
    "validationRules": [{"name": "camera_exists", "expression": "cameraId exists in live inventory"}]
  },
  {
    "id": "camera.enable",
    "version": "1.0.0",
    "parameters": {"type": "object", "required": ["cameraId"], "properties": {"cameraId": {"type": "string"}}},
    "reversibility": "reversible",
    "blastRadius": {"class": "bounded", "maxEntities": 1},
    "idempotencyKey": "id + cameraId",
    "dryRun": "optional",
    "sideEffects": "writes_system",
    "requiredScopes": ["camera.write"],
    "confirmation": "single",
    "rollback": "rollback_api",
    "examples": [{"intent": "Enable the south gate camera", "parameters": {"cameraId": "south-gate"}}],
    "validationRules": []
  },
  {
    "id": "archive.export",
    "version": "1.0.0",
    "parameters": {"type": "object", "required": ["cameraId", "from", "to"], "properties": {"cameraId": {"type": "string"}, "from": {"type": "string"}, "to": {"type": "string"}}},
    "reversibility": "compensable",
    "blastRadius": {"class": "scoped", "maxEntities": 1},
    "idempotencyKey": "id + cameraId + from + to",
    "dryRun": "optional",
    "sideEffects": "external_calls",
    "requiredScopes": ["archive.export"],
    "confirmation": "single",
    "rollback": "compensating_action",
    "examples": [{"intent": "Export last hour from camera 42", "parameters": {"cameraId": "42", "from": "now-1h", "to": "now"}}],
    "validationRules": []
  },
  {
    "id": "archive.delete",
    "version": "1.0.0",
    "parameters": {"type": "object", "required": ["cameraId", "before"], "properties": {"cameraId": {"type": "string"}, "before": {"type": "string"}}},
    "reversibility": "irreversible",
    "blastRadius": {"class": "bounded", "maxEntities": 1},
    "idempotencyKey": "id + cameraId + before",
    "dryRun": "mandatory",
    "sideEffects": "writes_system",
    "requiredScopes": ["archive.admin"],
    "confirmation": "typed",
    "rollback": "manual",
    "examples": [{"intent": "Delete old archive for camera 42", "parameters": {"cameraId": "42", "before": "2025-01-01"}}],
    "validationRules": [{"name": "retention_window_expired", "expression": "before is outside retention window"}]
  },
  {
    "id": "notification.broadcast",
    "version": "1.0.0",
    "parameters": {"type": "object", "required": ["message", "groupId"], "properties": {"message": {"type": "string"}, "groupId": {"type": "string"}}},
    "reversibility": "compensable",
    "blastRadius": {"class": "broad", "maxEntities": 1000},
    "idempotencyKey": "id + groupId + message_hash",
    "dryRun": "mandatory",
    "sideEffects": "notifications",
    "requiredScopes": ["notification.broadcast"],
    "confirmation": "multi_party",
    "rollback": "compensating_action",
    "examples": [{"intent": "Notify all operators about maintenance", "parameters": {"groupId": "operators", "message": "Maintenance starts at 22:00"}}],
    "validationRules": []
  },
  {
    "id": "user.role.assign",
    "version": "1.0.0",
    "parameters": {"type": "object", "required": ["userId", "role"], "properties": {"userId": {"type": "string"}, "role": {"type": "string"}}},
    "reversibility": "compensable",
    "blastRadius": {"class": "broad", "maxEntities": 500},
    "idempotencyKey": "id + userId + role",
    "dryRun": "mandatory",
    "sideEffects": "writes_system",
    "requiredScopes": ["iam.admin"],
    "confirmation": "multi_party",
    "rollback": "compensating_action",
    "examples": [{"intent": "Give Anna admin role", "parameters": {"userId": "anna", "role": "admin"}}],
    "validationRules": [{"name": "role_allowed", "expression": "role is assignable by operator"}]
  },
  {
    "id": "retention.policy.update",
    "version": "1.0.0",
    "parameters": {"type": "object", "required": ["days"], "properties": {"days": {"type": "integer", "minimum": 1}}},
    "reversibility": "irreversible",
    "blastRadius": {"class": "broad", "maxEntities": 10000},
    "idempotencyKey": "id + days",
    "dryRun": "mandatory",
    "sideEffects": "writes_system",
    "requiredScopes": ["archive.admin"],
    "confirmation": "cooling_off",
    "rollback": "manual",
    "examples": [{"intent": "Set archive retention to 90 days", "parameters": {"days": 90}}],
    "validationRules": []
  },
  {
    "id": "device.group.rename",
    "version": "1.0.0",
    "parameters": {"type": "object", "required": ["groupId", "name"], "properties": {"groupId": {"type": "string"}, "name": {"type": "string"}}},
    "reversibility": "reversible",
    "blastRadius": {"class": "scoped", "maxEntities": 1},
    "idempotencyKey": "id + groupId + name",
    "dryRun": "optional",
    "sideEffects": "writes_system",
    "requiredScopes": ["device.write"],
    "confirmation": "single",
    "rollback": "rollback_api",
    "examples": [{"intent": "Rename perimeter devices to Night perimeter", "parameters": {"groupId": "perimeter", "name": "Night perimeter"}}],
    "validationRules": []
  },
  {
    "id": "analytics.report.generate",
    "version": "1.0.0",
    "parameters": {"type": "object", "required": ["reportType"], "properties": {"reportType": {"type": "string"}}},
    "reversibility": "reversible",
    "blastRadius": {"class": "scoped", "maxEntities": 0},
    "idempotencyKey": "id + reportType",
    "dryRun": "not_applicable",
    "sideEffects": "read_only",
    "requiredScopes": ["analytics.read"],
    "confirmation": "automatic",
    "rollback": "rollback_api",
    "examples": [{"intent": "Generate daily camera health report", "parameters": {"reportType": "camera-health-daily"}}],
    "validationRules": []
  }
]
